import React from "react";

const NavigationPanel = (props) => {
    return (
        <div className='NavigationPanel'>
            <button className={styles.PanelButton} value='' onClick={null}></button>
            <button className={styles.PanelButton} value='' onClick={null}></button>
            <button className={styles.PanelButton} value='' onClick={null}></button>
            <button className={styles.PanelButton} value='' onClick={null}></button>
        </div>
    );
};

export default NavigationPanel;
